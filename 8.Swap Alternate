void swapAlternate(int *arr, int size)
{
  int temp = 0;
  for (int i = 0; i < size; i += 2) {

    int j = i + 1;

    if (i == size - 1) {

      break;
    }

    temp = arr[i];

    arr[i] = arr[j];

    arr[j] = temp;
  }
}
